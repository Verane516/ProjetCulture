package application;

/*
import domain.HallRepository;
import domain.Schedule;
import domain.Hall;
*/

// TODO: n'a pas de raison d'exister si Schedule est l'aggregate
public class HallService {
	//private HallRepository rep;
	
	// TODO: ça me semble plus approprié de pas avoir de repository au début et d'en charger un si besoin avec la foction load()
	/*public HallService(HallRepository rep) {
		this.rep = rep;
	}*/
	
	//ajouter un événement au planning d'une salle  (à modifier)
	
	// TODO: passer la salle en paramêtre, on modifie l'aggregate
	/*public int newEvent() {
		Hall h = new Hall(capacity, schedule);
		rep.add(h);
		return h.getId();
	}*/
	
	
}
