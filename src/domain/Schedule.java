package domain;

import java.util.Date;
import java.util.HashSet;

// collection de value object
public class Schedule extends HashSet<Slot>{
	// vérification qu'il n'y a pas de superposition de créneaux si on a le temps
}
